{% extends 'base.html.twig' %}

{% block title %}PrestationClient{% endblock %}

{% block body %}
    <h1>PrestationClient</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ prestation_client.id }}</td>
            </tr>
            <tr>
                <th>Heure_debut</th>
                <td>{{ prestation_client.heureDebut ? prestation_client.heureDebut|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Heure_fin</th>
                <td>{{ prestation_client.heureFin ? prestation_client.heureFin|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('prestation_client_index') }}">back to list</a>

    <a href="{{ path('prestation_client_edit', {'id': prestation_client.id}) }}">edit</a>

    {{ include('prestation_client/_delete_form.html.twig') }}
{% endblock %}