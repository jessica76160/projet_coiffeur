<form method="post" action="{{ path('prestation_composee_delete', {'id': prestation_composee.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prestation_composee.id) }}">
    <button class="btn">Delete</button>
</form>